# NutriSense Bot v2
# @NutriSenseUABot
# aiogram 3.x, SQLite, Monobank jar

import asyncio
import logging
import os
import json
import aiosqlite
import aiohttp
from datetime import datetime, timedelta

from aiogram import Bot, Dispatcher, F, Router
from aiogram.client.default import DefaultBotProperties
from aiogram.enums import ParseMode
from aiogram.filters import Command, CommandStart
from aiogram.fsm.context import FSMContext
from aiogram.fsm.state import State, StatesGroup
from aiogram.fsm.storage.memory import MemoryStorage
from aiogram.types import Message, CallbackQuery, ContentType
from aiogram.utils.keyboard import InlineKeyboardBuilder

try:
    from dotenv import load_dotenv
    load_dotenv()
except Exception:
    pass

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s | %(levelname)s | %(name)s | %(message)s"
)
log = logging.getLogger("NutriSense")

BOT_TOKEN = "8740306918:AAEDMvoW4ZJ5lSvrkd8NGmFaoavhXcc5EaA"
ADMIN_ID = 342045533
CHANNEL_ID = "@matmatias"
WEBHOOK_URL = os.getenv("WEBHOOK_URL", "")
MONO_JAR_URL = "https://send.monobank.ua/jar/8hUo6jMR5M"
DB_PATH = "nutrisense.db"

PLANS = {
    "start": {
        "name": "Start", "price_uah": 199, "days": 30, "emoji": "🌱",
        "features": [
            "📊 Розрахунок КБЖУ",
            "🍽 Меню на тиждень (сніданок, обід, вечеря)",
            "🧠 Тест харчової поведінки",
            "📚 28 тижнів освітнього контенту",
            "💡 Щоденна порада по харчуванню",
        ]
    },
    "premium": {
        "name": "Premium", "price_uah": 349, "days": 30, "emoji": "🌿",
        "features": [
            "✅ Все з Start",
            "🍱 Меню з перекусами + альтернативи страв",
            "📅 Щоденний трекер їжі",
            "📈 Щотижневий звіт прогресу",
            "🔄 Зміна меню в будь-який день",
            "💬 Пріоритетна підтримка",
        ]
    },
    "vip": {
        "name": "VIP", "price_uah": 549, "days": 30, "emoji": "👑",
        "features": [
            "✅ Все з Premium",
            "🎯 Персональне меню під ціль і смаки",
            "⚡ Меню на місяць одразу",
            "🏆 Необмежена зміна меню",
            "📱 VIP підтримка 24/7",
        ]
    }
}

ACTIVITY_LEVELS = {
    "sedentary":   (1.2,   "Малорухливий (офіс, без спорту)"),
    "light":       (1.375, "Легка активність (1-3 тренування/тиж)"),
    "moderate":    (1.55,  "Помірна активність (3-5 тренувань/тиж)"),
    "active":      (1.725, "Висока активність (6-7 тренувань/тиж)"),
    "very_active": (1.9,   "Дуже висока (спортсмен)"),
}

GOALS = {
    "lose":     ("Схуднення",      -300),
    "maintain": ("Підтримка ваги",    0),
    "gain":     ("Набір маси",     +300),
}

# ─── МЕНЮ НА ТИЖДЕНЬ ───────────────────────────────────────────────
# Базове меню (Start) — 7 днів, 3 прийоми
# Premium додає перекуси + альтернативи
# VIP генерує персоналізоване меню

MENU_ROTATION = {
    # ── ТИЖДЕНЬ 1 ──────────────────────────────────────────────
    1: {
        1: {"day": "Понеділок",
            "breakfast": ("Вівсянка 80г + чорниця 100г + мигдаль 20г + мед 1 ч.л.", 385),
            "lunch": ("Куряча грудка 180г + гречка 90г (сухої) + огірок 100г + оливкова олія 1 ч.л.", 525),
            "dinner": ("Філе тріски 200г + батат 150г + броколі 150г + лимон", 420),
            "snack1": ("Яблуко 180г + мигдаль 25г", 180),
            "snack2": ("Йогурт 2% 150г + насіння чіа 10г", 150)},
        2: {"day": "Вівторок",
            "breakfast": ("3 яйця омлет + шпинат 80г + помідор 100г + оливкова олія 1 ч.л.", 345),
            "lunch": ("Суп-пюре з сочевиці 300г + цільнозерновий хліб 50г", 485),
            "dinner": ("Індичка 180г тушкована + кабачок 200г + рис 80г (сухого)", 440),
            "snack1": ("Твердий сир 40г + огірок 150г", 165),
            "snack2": ("Кефір 1% 200мл + волоські горіхи 20г", 200)},
        3: {"day": "Середа",
            "breakfast": ("Гречка 80г (сухої) + 2 яйця круто + петрушка 10г", 360),
            "lunch": ("Лосось 200г запечений + булгур 80г (сухого) + руккола 60г + лимон", 565),
            "dinner": ("Куряче філе 180г гриль + цукіні 150г + перець 100г", 400),
            "snack1": ("Банан 130г + арахісова паста 15г", 190),
            "snack2": ("Сир кисломолочний 5% 150г + полуниця 80г", 170)},
        4: {"day": "Четвер",
            "breakfast": ("Тост ЦЗ 2 шт 80г + авокадо 80г + 2 яйця пашот + мікрозелень", 420),
            "lunch": ("Нут варений 200г + болгарський перець 100г + огірок 100г + оливкова олія 15мл", 500),
            "dinner": ("Тріска 200г + картопля варена 200г + зелень 20г + лимон", 415),
            "snack1": ("Грейпфрут 200г + насіння соняшника 20г", 145),
            "snack2": ("Кефір 200мл або протеїновий коктейль 250мл", 180)},
        5: {"day": "П'ятниця",
            "breakfast": ("Йогурт 2% 200г + гранола 50г + полуниця 100г", 390),
            "lunch": ("Паста ТСП 80г (сухої) + куряча грудка 150г + томати 150г + базилік", 540),
            "dinner": ("Яловичина нежирна 180г + гречка 80г (сухої) + огірок 100г + оливкова олія 1 ч.л.", 480),
            "snack1": ("Морква 150г + хумус 50г", 150),
            "snack2": ("Волоські горіхи 30г", 195)},
        6: {"day": "Субота",
            "breakfast": ("Вівсяні млинці 3шт (борошно 70г + 1 яйце + молоко 100мл) + мед 15г", 410),
            "lunch": ("Борщ домашній 350мл + яловичина 150г + хліб ЦЗ 30г", 510),
            "dinner": ("Креветки 200г + рис 80г (сухого) + броколі 150г + соєвий соус 1 ч.л.", 430),
            "snack1": ("Фруктовий салат: яблуко 100г + груша 100г + ківі 80г", 130),
            "snack2": ("Сир кисломолочний 9% 150г + малина 80г", 165)},
        7: {"day": "Неділя",
            "breakfast": ("Сирники 3шт (сир 200г + 1 яйце + борошно 20г) + чорниця 100г", 385),
            "lunch": ("Куряча гомілка 250г запечена + булгур 80г + кабачок 150г", 555),
            "dinner": ("Риба на парі 200г + гречка 80г (сухої) + листовий салат 80г + лимон", 405),
            "snack1": ("Смузі: шпинат 50г + яблуко 150г + імбир 5г + вода 200мл", 120),
            "snack2": ("Чорний шоколад 70% 20г + мигдаль 20г", 210)},
    },
    # ── ТИЖДЕНЬ 2 ──────────────────────────────────────────────
    2: {
        1: {"day": "Понеділок",
            "breakfast": ("2 яйця смажені + томати 150г + зелена цибуля 20г + хліб ЦЗ 30г", 315),
            "lunch": ("Курячий бульйон 300мл + локшина 60г + морква 80г + зелень", 490),
            "dinner": ("Скумбрія 200г запечена + картопля 200г + петрушка 15г", 450),
            "snack1": ("Груша 180г + волоські горіхи 25г", 175),
            "snack2": ("Грецький йогурт 0% 150г", 140)},
        2: {"day": "Вівторок",
            "breakfast": ("Пшоняна каша 80г (сухої) + гарбуз 150г + мед 1 ч.л.", 370),
            "lunch": ("Індичка 180г + рататуй (кабачок 100г + баклажан 100г + томати 100г) + рис 70г", 535),
            "dinner": ("Тефтелі 3шт (курка 200г + цибуля 50г + рис 30г) + капуста тушкована 200г", 415),
            "snack1": ("Апельсин 180г + мигдаль 20г", 175),
            "snack2": ("Кефір 1% 200мл", 90)},
        3: {"day": "Середа",
            "breakfast": ("Тост житній 2шт 80г + авокадо 70г + яйце пашот + лимон", 400),
            "lunch": ("Квасоля варена 200г + куряча грудка 150г + томати 150г + оливкова олія 1 ч.л.", 510),
            "dinner": ("Форель 200г запечена + спаржа 150г + лимон + розмарин", 435),
            "snack1": ("Селера 150г + арахісова паста 20г", 160),
            "snack2": ("Сир кисломолочний 5% 150г + чорниця 80г", 165)},
        4: {"day": "Четвер",
            "breakfast": ("Вівсянка 80г + банан 130г + кориця + мигдальне молоко 150мл", 400),
            "lunch": ("Гречаний суп 350мл (гречка 50г + гриби 100г + цибуля 50г) + хліб ЦЗ 30г", 475),
            "dinner": ("Куряча грудка 180г + цвітна капуста 200г + гречка 70г (сухої)", 440),
            "snack1": ("Ківі 2шт 160г + насіння льону 10г", 145),
            "snack2": ("Ряжанка 200мл", 120)},
        5: {"day": "П'ятниця",
            "breakfast": ("Омлет 3 яйця + куряча грудка 80г + болгарський перець 100г", 360),
            "lunch": ("Паста ТСП 80г + мідії 200г + томатний соус 100г + базилік", 550),
            "dinner": ("Яловичий стейк нежирний 180г + перець гриль 150г + кабачок гриль 150г", 490),
            "snack1": ("Ягоди змішані 200г", 80),
            "snack2": ("Горіхова суміш 30г (мигдаль + кеш'ю + фундук)", 185)},
        6: {"day": "Субота",
            "breakfast": ("Тост ЦЗ 2шт 80г + яйце 1шт + сир кисломолочний 80г + зелень", 390),
            "lunch": ("Вареники з картоплею 6шт 300г + сметана 10% 30г + цибуля смажена 30г", 500),
            "dinner": ("Лосось 200г + кускус 80г (сухого) + болгарський перець 100г + оливкова олія 1 ч.л.", 460),
            "snack1": ("Яблуко 180г + сир кисломолочний 100г", 180),
            "snack2": ("Молоко тепле 200мл + мед 1 ч.л.", 110)},
        7: {"day": "Неділя",
            "breakfast": ("Налисники 3шт (борошно 60г + 2 яйця + молоко 150мл) + сир 150г + зелень", 420),
            "lunch": ("Курячий шашлик 250г + овочі гриль 200г + лаваш 60г", 560),
            "dinner": ("Рибні котлети 3шт (риба 200г + цибуля 50г + яйце) + картопляне пюре 200г + салат 80г", 435),
            "snack1": ("Банан 130г + чорний шоколад 70% 15г", 195),
            "snack2": ("Кефір 200мл + насіння чіа 10г", 130)},
    },
    # ── ТИЖДЕНЬ 3 ──────────────────────────────────────────────
    3: {
        1: {"day": "Понеділок",
            "breakfast": ("Рисова каша 80г (сухої) + кокосове молоко 100мл + манго 150г", 395),
            "lunch": ("Крем-суп з броколі 300г + куряча грудка 150г + хліб ЦЗ 30г", 505),
            "dinner": ("Дорада 250г запечена + лимон + чебрець + оливкова олія 1 ч.л.", 415),
            "snack1": ("Нектарин 180г + фісташки 25г", 185),
            "snack2": ("Йогурт 2% 150г + полуниця 100г", 145)},
        2: {"day": "Вівторок",
            "breakfast": ("Тост ЦЗ 2шт 80г + лосось слабосолоний 80г + вершковий сир 30г + огірок", 415),
            "lunch": ("Нут варений 150г + куряча грудка 150г + руккола 60г + помідори черрі 80г + оливкова олія 15мл", 520),
            "dinner": ("Котлети з індички 3шт 200г + гречка 80г (сухої) + огірок 120г", 440),
            "snack1": ("Помело 200г + кеш'ю 25г", 165),
            "snack2": ("Простокваша 200мл", 100)},
        3: {"day": "Середа",
            "breakfast": ("Булгур 80г (сухого) + 2 яйця + помідор 100г + огірок 100г + зелень", 375),
            "lunch": ("Суп харчо 300мл (яловичина 100г + рис 40г + томати + спеції) + хліб ЦЗ 30г", 495),
            "dinner": ("Тунець консервований у власному соку 150г + рис 80г + зелений горошок 100г", 420),
            "snack1": ("Грейпфрут 200г + гарбузове насіння 20г", 140),
            "snack2": ("Сир кисломолочний 5% 150г + мед 1 ч.л.", 155)},
        4: {"day": "Четвер",
            "breakfast": ("3 яйця скрембл + лосось 80г + зелена цибуля 20г + хліб ЦЗ 30г", 430),
            "lunch": ("Кабачок 200г + баклажан 150г + перець 100г + куряча грудка 150г + хліб ЦЗ 30г", 515),
            "dinner": ("Куряче філе 180г у кефірі 100мл + рис 80г (сухого) + листовий салат 80г", 435),
            "snack1": ("Ківі 2шт 160г + мигдаль 15г", 155),
            "snack2": ("Кефір 200мл + кориця", 95)},
        5: {"day": "П'ятниця",
            "breakfast": ("Вівсяні млинці 3шт (борошно 70г + 1 яйце) + ягоди 100г + сметана 10% 30г", 405),
            "lunch": ("Плов 350г (курка 150г + рис 80г + морква 80г + цибуля 50г + спеції)", 545),
            "dinner": ("Судак 200г запечений + болгарський перець 100г + кабачок 150г + лимон", 400),
            "snack1": ("Морква 150г + хумус домашній 50г", 155),
            "snack2": ("Йогурт 2% 150г + насіння чіа 10г + малина 80г", 160)},
        6: {"day": "Субота",
            "breakfast": ("3 яйця + гриби 100г + цибуля 50г + твердий сир 30г + оливкова олія 1 ч.л.", 380),
            "lunch": ("Шурпа 350мл (яловичина 150г + картопля 150г + морква 80г + цибуля 50г)", 530),
            "dinner": ("Болгарський перець 2шт фарширований (фарш 150г + рис 50г + томати) + сметана 20г", 445),
            "snack1": ("Суниця 200г", 65),
            "snack2": ("Горіхово-ягідна суміш 40г (горіхи 25г + родзинки 15г)", 200)},
        7: {"day": "Неділя",
            "breakfast": ("Пудинг: насіння чіа 40г + мигдальне молоко 200мл + банан 100г + мед 10г", 365),
            "lunch": ("Піца ЦЗ (тісто 150г + куряча грудка 100г + томати 100г + сир 50г + базилік)", 555),
            "dinner": ("Мідії 300г тушковані + томати 150г + часник + петрушка + хліб ЦЗ 30г", 390),
            "snack1": ("Груша 180г + бринза 35г", 175),
            "snack2": ("Ряжанка 200мл + насіння льону 10г", 125)},
    },
    # ── ТИЖДЕНЬ 4 ──────────────────────────────────────────────
    4: {
        1: {"day": "Понеділок",
            "breakfast": ("Смузі-боул: банан 130г + чорниця 100г + гранола 50г + кокосове молоко 100мл", 415),
            "lunch": ("Фрикадельки 3шт (курка 200г + цибуля 50г) + паста ТСП 80г + томатний соус 100г", 535),
            "dinner": ("Камбала 250г + броколі 150г + лимон + оливкова олія 1 ч.л.", 405),
            "snack1": ("Абрикоси 3шт 120г + волоські горіхи 20г", 170),
            "snack2": ("Грецький йогурт 2% 150г + мед 10г", 150)},
        2: {"day": "Вівторок",
            "breakfast": ("Кіноа 80г (сухої) + 2 яйця + помідор 100г + огірок 100г + оливкова олія 1 ч.л.", 385),
            "lunch": ("Суп з куркою 300мл (грудка 100г + локшина 50г + морква 80г) + хліб ЦЗ 30г", 500),
            "dinner": ("Яловичина нежирна 180г тушкована + картопля 200г + зелень", 460),
            "snack1": ("Персик 180г + мигдаль 20г", 180),
            "snack2": ("Кефір 200мл + куркума щіпка", 100)},
        3: {"day": "Середа",
            "breakfast": ("3 яйця омлет + цукіні 100г + пармезан 20г + базилік", 360),
            "lunch": ("Лосось 200г + булгур 80г (сухого) + руккола 60г + авокадо 60г + лимон", 545),
            "dinner": ("Куряча грудка 180г у горіховому соусі 30г + рис 80г + зелень", 470),
            "snack1": ("Диня 250г + насіння гарбуза 20г", 145),
            "snack2": ("Йогурт 2% 150г + насіння льону 10г", 140)},
        4: {"day": "Четвер",
            "breakfast": ("Вівсянка 80г + яблуко терте 150г + кориця + мигдальне молоко 150мл", 370),
            "lunch": ("Крем-суп гарбузовий 300г + куряча грудка 100г + вершки 10% 30мл + насіння", 510),
            "dinner": ("Тілапія 200г + рис 80г (сухого) + листовий салат 80г + лимон", 415),
            "snack1": ("Сливи 3шт 150г + кеш'ю 20г", 165),
            "snack2": ("Ряжанка 200мл + насіння чіа 10г", 125)},
        5: {"day": "П'ятниця",
            "breakfast": ("2 яйця пашот + тост ЦЗ 2шт 80г + авокадо 80г + мікрозелень + лимон", 425),
            "lunch": ("Паелья 350г (мідії 100г + креветки 100г + рис 80г + перець 80г + томати 80г)", 555),
            "dinner": ("Куряче філе 180г у кунжуті 15г + гречка 80г (сухої) + огірок 120г", 445),
            "snack1": ("Виноград 150г + бринза 35г", 175),
            "snack2": ("Кефір 200мл + чорниця 80г + мед 1 ч.л.", 130)},
        6: {"day": "Субота",
            "breakfast": ("Рикотта 150г + мед 15г + горіхи 20г + персик 150г", 400),
            "lunch": ("Стейк куриний 250г + запечений гарбуз 200г + руккола 60г + соус з йогурту 50г", 540),
            "dinner": ("Морська риба 250г (в фользі з цибулею 80г + помідорами 100г + лимоном)", 420),
            "snack1": ("Кавун 300г", 90),
            "snack2": ("Суміш горіхів і сухофруктів 35г", 190)},
        7: {"day": "Неділя",
            "breakfast": ("Вафлі 2шт (борошно вівсяне 70г + 1 яйце + молоко 100мл) + ягоди 100г + йогурт 100г", 410),
            "lunch": ("Курячий карі 350г (грудка 180г + кокосове молоко 100мл + спеції + цибуля 80г) + рис 80г", 560),
            "dinner": ("Лосось 200г + кускус 80г (сухого) + огірок 100г + руккола 60г + лимон", 455),
            "snack1": ("Інжир 2шт 80г + мигдаль 20г", 195),
            "snack2": ("Сир кисломолочний 5% 150г + ваніль + мед 1 ч.л.", 145)},
    },
}

# ─── СТАНИ ─────────────────────────────────────────────────────────
class KBJUState(StatesGroup):
    waiting_gender   = State()
    waiting_age      = State()
    waiting_weight   = State()
    waiting_height   = State()
    waiting_activity = State()
    waiting_goal     = State()

class BehaviorTestState(StatesGroup):
    waiting_answer = State()

class AdminBroadcastState(StatesGroup):
    waiting_text = State()

# ─── DATABASE ──────────────────────────────────────────────────────
async def init_db():
    async with aiosqlite.connect(DB_PATH) as db:
        await db.executescript("""
            CREATE TABLE IF NOT EXISTS users (
                user_id     INTEGER PRIMARY KEY,
                username    TEXT,
                full_name   TEXT,
                plan        TEXT DEFAULT 'free',
                plan_until  TEXT,
                joined_at   TEXT DEFAULT CURRENT_TIMESTAMP,
                week        INTEGER DEFAULT 1,
                gender      TEXT,
                age         INTEGER,
                weight      REAL,
                height      REAL,
                activity    REAL,
                goal        TEXT,
                calories    INTEGER,
                protein     REAL,
                fat         REAL,
                carbs       REAL,
                menu_day    INTEGER DEFAULT 1
            );
            CREATE TABLE IF NOT EXISTS payments (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                user_id     INTEGER,
                plan        TEXT,
                amount_uah  INTEGER,
                invoice_id  TEXT,
                status      TEXT DEFAULT 'pending',
                created_at  TEXT DEFAULT CURRENT_TIMESTAMP
            );
            CREATE TABLE IF NOT EXISTS tracker (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                user_id     INTEGER,
                date        TEXT,
                meal        TEXT,
                note        TEXT,
                created_at  TEXT DEFAULT CURRENT_TIMESTAMP
            );
        """)
        await db.commit()
    log.info("DB initialized")

async def get_user(user_id: int):
    async with aiosqlite.connect(DB_PATH) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute("SELECT * FROM users WHERE user_id = ?", (user_id,)) as cur:
            row = await cur.fetchone()
            return dict(row) if row else None

async def upsert_user(user_id: int, **kwargs):
    async with aiosqlite.connect(DB_PATH) as db:
        existing = await get_user(user_id)
        if not existing:
            await db.execute("INSERT INTO users (user_id) VALUES (?)", (user_id,))
        if kwargs:
            sets = ", ".join(f"{k} = ?" for k in kwargs)
            vals = list(kwargs.values()) + [user_id]
            await db.execute(f"UPDATE users SET {sets} WHERE user_id = ?", vals)
        await db.commit()

async def get_all_users():
    async with aiosqlite.connect(DB_PATH) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute("SELECT * FROM users") as cur:
            rows = await cur.fetchall()
            return [dict(r) for r in rows]

async def get_stats():
    async with aiosqlite.connect(DB_PATH) as db:
        async with db.execute("SELECT COUNT(*) FROM users") as c:
            total = (await c.fetchone())[0]
        async with db.execute("SELECT COUNT(*) FROM users WHERE plan != 'free'") as c:
            paid = (await c.fetchone())[0]
        async with db.execute("SELECT SUM(amount_uah) FROM payments WHERE status = 'paid'") as c:
            revenue = (await c.fetchone())[0] or 0
    return {"total": total, "paid": paid, "revenue": revenue}

async def save_payment(user_id, plan, amount, invoice_id):
    async with aiosqlite.connect(DB_PATH) as db:
        await db.execute(
            "INSERT INTO payments (user_id, plan, amount_uah, invoice_id) VALUES (?,?,?,?)",
            (user_id, plan, amount, invoice_id)
        )
        await db.commit()

def has_access(user, required):
    order = {"free": 0, "start": 1, "premium": 2, "vip": 3}
    if not user:
        return False
    plan = user.get("plan", "free")
    until_str = user.get("plan_until")
    if plan != "free" and until_str:
        until = datetime.strptime(until_str, "%Y-%m-%d %H:%M:%S")
        if datetime.now() > until:
            return False
    return order.get(plan, 0) >= order.get(required, 0)

def is_admin(user_id):
    return user_id == ADMIN_ID

# ─── КБЖУ ──────────────────────────────────────────────────────────
def calculate_kbju(gender, age, weight, height, activity, goal):
    if gender == "female":
        bmr = 10 * weight + 6.25 * height - 5 * age - 161
    else:
        bmr = 10 * weight + 6.25 * height - 5 * age + 5
    tdee = bmr * activity
    calories = round(tdee + GOALS[goal][1])
    protein = round(weight * 1.7)
    fat = round(calories * 0.28 / 9)
    carbs = round((calories - protein * 4 - fat * 9) / 4)
    return {"bmr": round(bmr), "tdee": round(tdee),
            "calories": calories, "protein": protein, "fat": fat, "carbs": carbs}

def build_meal_plan(calories, protein, fat, carbs):
    dist = {
        "🌅 Сніданок":  (0.30, 0.25, 0.35, 0.25),
        "🍱 Перекус 1": (0.10, 0.15, 0.10, 0.10),
        "☀️ Обід":      (0.35, 0.35, 0.30, 0.40),
        "🍎 Перекус 2": (0.10, 0.15, 0.10, 0.10),
        "🌙 Вечеря":    (0.15, 0.10, 0.15, 0.15),
    }
    lines = []
    for meal, (kc, pr, ft, cb) in dist.items():
        lines.append(
            f"{meal}: {round(calories*kc)} ккал "
            f"| Б:{round(protein*pr)}г Ж:{round(fat*ft)}г В:{round(carbs*cb)}г"
        )
    return "\n".join(lines)

def generate_menu_for_user(user, day_num, plan, week_rotation=1):
    # Rotation: week 1-7 = menu 1, 8-14 = menu 2, 15-21 = menu 3, 22-28 = menu 4
    menu_week = ((week_rotation - 1) % 4) + 1
    menu = MENU_ROTATION.get(menu_week, MENU_ROTATION[1])
    day = menu.get(day_num, menu[1])
    calories = user.get("calories", 2000)
    goal = user.get("goal", "maintain")
    goal_label = GOALS.get(goal, ("Підтримка",))[0]

    text = f"🍽 <b>Меню на {day['day']}</b>\n"
    text += f"🎯 Ціль: {goal_label} | 🔥 {calories} ккал/день\n\n"

    text += f"🌅 <b>Сніданок</b>\n{day['breakfast'][0]}\n~{day['breakfast'][1]} ккал\n\n"
    text += f"☀️ <b>Обід</b>\n{day['lunch'][0]}\n~{day['lunch'][1]} ккал\n\n"
    text += f"🌙 <b>Вечеря</b>\n{day['dinner'][0]}\n~{day['dinner'][1]} ккал\n\n"

    if plan in ("premium", "vip"):
        text += f"🍎 <b>Перекус 1</b>\n{day['snack1'][0]}\n~{day['snack1'][1]} ккал\n\n"
        text += f"🍎 <b>Перекус 2</b>\n{day['snack2'][0]}\n~{day['snack2'][1]} ккал\n\n"

    total = day['breakfast'][1] + day['lunch'][1] + day['dinner'][1]
    if plan in ("premium", "vip"):
        total += day['snack1'][1] + day['snack2'][1]

    text += f"📊 Всього: ~{total} ккал з {calories} ккал\n"
    diff = calories - total
    if diff > 0:
        text += f"✅ Залишок: {diff} ккал — можна додати перекус\n"
    elif diff < -50:
        text += f"⚠️ Трохи вище норми на {abs(diff)} ккал\n"
    else:
        text += f"✅ Ідеальний баланс!\n"

    return text

# ─── КЛАВІАТУРИ ────────────────────────────────────────────────────
def kb_main_menu(plan="free"):
    b = InlineKeyboardBuilder()
    b.button(text="📊 Розрахувати КБЖУ", callback_data="kbju_start")
    b.button(text="🍽 Меню на сьогодні", callback_data="menu_today")
    b.button(text="🧠 Тест поведінки", callback_data="behavior_test")
    b.button(text="📚 Контент тижня", callback_data="weekly_content")
    if plan in ("premium", "vip"):
        b.button(text="📅 Трекер їжі", callback_data="tracker")
        b.button(text="📈 Мій прогрес", callback_data="my_progress")
    b.button(text="💎 Тарифи", callback_data="plans")
    b.button(text="👤 Мій профіль", callback_data="my_profile")
    b.adjust(2, 2, 2, 2)
    return b.as_markup()

def kb_plans():
    b = InlineKeyboardBuilder()
    b.button(text="🌱 Start — 199 грн/міс", callback_data="buy_start")
    b.button(text="🌿 Premium — 349 грн/міс", callback_data="buy_premium")
    b.button(text="👑 VIP — 549 грн/міс", callback_data="buy_vip")
    b.button(text="◀️ Назад", callback_data="main_menu")
    b.adjust(1)
    return b.as_markup()

def kb_confirm_buy(plan):
    b = InlineKeyboardBuilder()
    b.button(text="💳 Оплатити", callback_data=f"pay_card_{plan}")
    b.button(text="◀️ Назад", callback_data="plans")
    b.adjust(1)
    return b.as_markup()

def kb_activity():
    b = InlineKeyboardBuilder()
    for key, (_, label) in ACTIVITY_LEVELS.items():
        b.button(text=label, callback_data=f"activity_{key}")
    b.adjust(1)
    return b.as_markup()

def kb_goal():
    b = InlineKeyboardBuilder()
    for key, (label, _) in GOALS.items():
        b.button(text=label, callback_data=f"goal_{key}")
    b.adjust(1)
    return b.as_markup()

def kb_gender():
    b = InlineKeyboardBuilder()
    b.button(text="👩 Жінка", callback_data="gender_female")
    b.button(text="👨 Чоловік", callback_data="gender_male")
    b.adjust(2)
    return b.as_markup()

def kb_back_main():
    b = InlineKeyboardBuilder()
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)
    return b.as_markup()

def kb_admin():
    b = InlineKeyboardBuilder()
    b.button(text="📊 Статистика", callback_data="adm_stats")
    b.button(text="👥 Всі юзери", callback_data="adm_users")
    b.button(text="📢 Розсилка", callback_data="adm_broadcast")
    b.button(text="📤 Контент всім", callback_data="adm_send_content")
    b.adjust(2)
    return b.as_markup()

def kb_menu_actions(plan):
    b = InlineKeyboardBuilder()
    b.button(text="📅 Меню на тиждень", callback_data="menu_week")
    if plan in ("premium", "vip"):
        b.button(text="🔄 Змінити меню", callback_data="menu_change")
    b.button(text="➡️ Наступний день", callback_data="menu_next")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)
    return b.as_markup()

def kb_after_kbju(plan):
    b = InlineKeyboardBuilder()
    b.button(text="🍽 Отримати меню", callback_data="menu_today")
    if plan == "free":
        b.button(text="🔓 Розблокувати повний план", callback_data="plans")
    b.button(text="🧠 Пройти тест поведінки", callback_data="behavior_test")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)
    return b.as_markup()

def kb_behavior_test(q_num):
    options = {
        0: [("👍 Так, часто", "bt_yes"), ("🤔 Іноді", "bt_sometimes"), ("👎 Рідко", "bt_no")],
        1: [("🟢 Наростає поступово", "bt_gradual"), ("🔴 Раптово", "bt_sudden"), ("⚪ По-різному", "bt_varies")],
        2: [("😔 Від стресу", "bt_stress"), ("😴 Від нудьги", "bt_bored"), ("😋 Від запаху/вигляду", "bt_sensory")],
        3: [("✅ Легко", "bt_easy"), ("⚠️ Важко, але можу", "bt_hard"), ("❌ Не можу", "bt_cant")],
        4: [("😔 Часто соромлюсь", "bt_shame"), ("😌 Іноді", "bt_sometimes2"), ("😊 Рідко", "bt_never")],
    }
    b = InlineKeyboardBuilder()
    for text, cb in options.get(q_num, []):
        b.button(text=text, callback_data=cb)
    b.adjust(1)
    return b.as_markup()

BEHAVIOR_QUESTIONS = [
    "1️⃣ Чи буває що їси без фізичного голоду\n(від нудьги, стресу, емоцій)?",
    "2️⃣ Як зазвичай з'являється голод?",
    "3️⃣ Що найчастіше провокує їсти не в свій час?",
    "4️⃣ Чи вдається зупинитись після першого шматочка?",
    "5️⃣ Чи виникає сором або провина після їжі?",
]

def analyze_behavior(answers):
    score = 0
    has_emotional = "bt_yes" in answers
    has_sometimes = "bt_sometimes" in answers
    sudden_hunger = "bt_sudden" in answers
    stress_trigger = "bt_stress" in answers
    boredom_trigger = "bt_bored" in answers
    sensory_trigger = "bt_sensory" in answers
    cant_stop = "bt_cant" in answers
    shame_after = "bt_shame" in answers

    if has_emotional: score += 2
    if has_sometimes: score += 1
    if sudden_hunger: score += 2
    if stress_trigger: score += 2
    if boredom_trigger: score += 1
    if cant_stop: score += 3
    if shame_after: score += 2

    if score <= 3:
        detail = ""
        if sensory_trigger:
            detail = "\n\n🔍 <b>Нюанс:</b> Тебе можуть провокувати запахи і вигляд їжі — це нормально. Це нейробіологія, не слабкість."
        return ("🟢", "Природний їдець",
            "Це рідкість. Справді.\n\n"
            "Більшість людей втрачають зв'язок з тілом ще в дитинстві — через правило 'їж поки не з'їж', "
            "заохочення солодким, заборони.\n\n"
            "Ти зберіг(ла) або відновив(ла) цей контакт.\n\n"
            "<b>Що це означає на практиці:</b>\n"
            "→ Твій апетит — надійний навігатор\n"
            "→ Ти зупиняєшся природно, без підрахунку\n"
            "→ Їжа не несе емоційного навантаження\n\n"
            "<b>Твоє завдання:</b> не ускладнювати. "
            "Основна пастка для твого типу — почати рахувати калорії і зламати природний механізм."
            + detail)
    elif score <= 5:
        triggers = []
        if stress_trigger: triggers.append("стрес і тривога")
        if boredom_trigger: triggers.append("нудьга і порожнеча")
        if sensory_trigger: triggers.append("запахи і вигляд їжі")
        trigger_str = ", ".join(triggers) if triggers else "зовнішні тригери"
        return ("🟡", "Ситуативний їдець",
            f"Більшість часу ти харчуєшся добре. Але є конкретні тригери: <b>{trigger_str}</b>.\n\n"
            "Це не розлад харчової поведінки. Це вивчена реакція — мозок знає, що їжа швидко "
            "знижує кортизол і дофамін. Він просто використовує найшвидший інструмент.\n\n"
            "<b>Що відбувається в мозку:</b>\n"
            "Тригер → дискомфорт → їжа → короткочасне полегшення → повторення\n\n"
            "<b>Три точки втручання:</b>\n"
            "① Пауза 5 хвилин між тригером і дією — мозок перемикається\n"
            "② Регулярні прийоми їжі знижують базовий рівень тривоги\n"
            "③ Заміна не заборона — знайди 2-3 некалорійних заспокоювача (прогулянка, холодна вода, дихання)\n\n"
            "Структуроване меню усуває 60% ситуативних зривів автоматично.")
    elif score <= 8:
        return ("🟡", "Гедонічний їдець",
            "Їжа для тебе — це більше ніж паливо. Це задоволення, нагорода, спосіб перемкнутися.\n\n"
            "І це не погано. Гедонічне харчування вбудоване в нашу біологію. "
            "Проблема виникає коли воно стає єдиним способом регулювати стан.\n\n"
            "<b>Що показує твій профіль:</b>\n"
            "→ Голод виникає раптово — це ознака нерівномірного рівня цукру\n"
            "→ Важко зупинитися — рецептори насичення запізнюються на 15-20 хвилин\n"
            "→ Провина після їжі — це не совість, це кортизол після переїдання\n\n"
            "<b>Що реально допомагає:</b>\n"
            "① Їж кожні 3-4 год — знімає різкі стрибки апетиту\n"
            "② Починай прийом їжі з білку і овочів — уповільнює всмоктування цукру\n"
            "③ Дозволь собі улюблене — заборона підсилює тягу в 3 рази (нейробіологія)\n"
            "④ Їж повільно — 20 хвилин для сигналу ситості\n\n"
            "Готове меню з правильним розподілом калорій вирівняє рівень цукру протягом тижня.")
    else:
        chaos_type = ""
        if cant_stop and shame_after:
            chaos_type = (
                "\n\n🔍 <b>Важливо:</b> Поєднання 'не можу зупинитися' + 'сором після' — "
                "це класична ознака циклу обмеження-зриву. "
                "Тіло не отримує достатньо → мозок компенсує переїданням → сором → нове обмеження. "
                "Вихід не в 'більше сили волі', а в достатній калорійності щодня."
            )
        return ("🔴", "Дисрегульований їдець",
            "Це чесний результат. І він пояснює багато.\n\n"
            "Дисрегульоване харчування — не про слабку волю. "
            "Це про те, що тіло і мозок розірвали зв'язок через тривалий стрес, "
            "обмежувальні дієти або нерегулярне харчування.\n\n"
            "<b>Що зараз відбувається в тілі:</b>\n"
            "→ Рівень кортизолу хронічно підвищений\n"
            "→ Лептин (гормон ситості) не спрацьовує вчасно\n"
            "→ Мозок постійно сканує середовище на їжу — це виснажує\n\n"
            "<b>Три кроки відновлення:</b>\n"
            "① <b>Регулярність понад все</b> — їж кожні 3-4 год, навіть без голоду. "
            "Це перепрограмує гормональні сигнали за 2-3 тижні\n"
            "② <b>Достатньо калорій</b> — дефіцит посилює дисрегуляцію. Спочатку стабілізація, потім ціль\n"
            "③ <b>Прибери заборони</b> — дозволена їжа перестає бути обсесією\n\n"
            "Готове меню — це не дієта. Це структура, якої зараз потребує твій мозок."
            + chaos_type)

WEEKLY_CONTENT = {
    1: {"theme": "🌱 Основи усвідомленого харчування", "posts": [
        {"day": 1, "title": "Що таке усвідомлене харчування?",
         "text": "🧠 <b>Тиждень 1 · День 1</b>\n\n<b>Усвідомлене харчування — це не дієта.</b>\n\nЦе вміння чути своє тіло:\n→ їсти, коли є фізичний голод\n→ зупинятися при насиченні\n→ обирати їжу, яка дає енергію і задоволення\n\nФормула:\nЇм за голодом + зупиняюся за насиченням + не думаю про їжу між прийомами 🌿"},
        {"day": 3, "title": "Шкала голоду-ситості",
         "text": "🌡 <b>Тиждень 1 · День 3</b>\n\n<b>Шкала 1-10:</b>\n\n1-3 — Голод, треба їсти\n4 ← <b>починай їсти тут</b>\n5-6 — Легка ситість ← <b>зупиняйся тут</b>\n7-10 — Переїв\n\n<b>Практика:</b> перевіряй рівень перед кожним прийомом їжі 🌿"},
        {"day": 5, "title": "Ідеальна тарілка",
         "text": "🍽 <b>Тиждень 1 · День 5</b>\n\n🥗 1/2 — різнокольорові овочі\n🍗 1/4 — білок\n🌾 1/4 — складні вуглеводи\n🫒 невелика кількість — корисні жири\n\nЦя формула лежить в основі твого меню в боті 🌿"},
    ]},
    2: {"theme": "🥗 Макронутрієнти: Білок", "posts": [
        {"day": 1, "title": "Навіщо нам білок?",
         "text": "💪 <b>Тиждень 2 · День 1</b>\n\nДефіцит білка:\n→ випадіння волосся\n→ збої в гормональній системі\n→ зниження імунітету\n\nНорма: 1.4-2.0 г на кг ваги\nТвоя норма вже розрахована в профілі 📊"},
        {"day": 3, "title": "Джерела білку",
         "text": "🐟 <b>Тиждень 2 · День 3</b>\n\n<b>НЕЖИРНИЙ</b> ✅\nКурка, індичка, тріска, яєчний білок, сир 2%\n\n<b>ЖИРНИЙ</b> ✅\nЛосось, яйця, скумбрія, сир 9%\n\nОбидва варіанти є в твоєму меню 💚"},
    ]},
    3: {"theme": "🧈 Макронутрієнти: Жири", "posts": [
        {"day": 1, "title": "Жири — не ворог",
         "text": "🥑 <b>Тиждень 3 · День 1</b>\n\nЖири — основа гормонів і мозку.\n\nКорисні жири:\n✅ Оливкова олія, авокадо, горіхи\n✅ Лосось, скумбрія (Омега-3)\n\nВсе це вже включено в твоє меню 🌿"},
    ]},
    4: {"theme": "🌾 Вуглеводи", "posts": [
        {"day": 1, "title": "Складні вуглеводи — твоя енергія",
         "text": "⚡ <b>Тиждень 4 · День 1</b>\n\nСкладні вуглеводи дають тривалу енергію:\n🌾 Гречка, овес, рис, булгур\n🫘 Бобові\n🍠 Батат\n\nПрості вуглеводи (цукор, солодощі) — в мінімальній кількості 💚"},
    ]},
    5: {"theme": "🧠 Харчова поведінка", "posts": [
        {"day": 1, "title": "3 типи голоду",
         "text": "🧠 <b>Тиждень 5 · День 1</b>\n\n🟢 Фізичний — наростає поступово, їж!\n🟡 Гедонічний — від емоцій, зроби паузу\n🔴 Дисрегульований — без насичення, потрібна структура\n\nТвій результат тесту вже в профілі 🌿"},
    ]},
    6: {"theme": "😴 Сон і харчування", "posts": [
        {"day": 1, "title": "Як сон впливає на вагу",
         "text": "😴 <b>Тиждень 6 · День 1</b>\n\nПри нестачі сну зростає грелін (голод) і знижується лептин (ситість).\n\n✅ 7-9 годин сну\n✅ Стабільний час\n✅ Вечеря за 2-3 год до сну 🌿"},
    ]},
    7: {"theme": "🦠 Мікробіом", "posts": [
        {"day": 1, "title": "Мікробіом і апетит",
         "text": "🦠 <b>Тиждень 7 · День 1</b>\n\n90% серотоніну — в кишківнику!\n\nЩо підтримує:\n✅ Йогурт, кефір, квашена капуста\n✅ Клітковина щодня\n✅ Різноманітне харчування 💚"},
    ]},
    8: {"theme": "🍽 Сніданок", "posts": [
        {"day": 1, "title": "Ідеальний сніданок",
         "text": "🌅 <b>Тиждень 8 · День 1</b>\n\nІдеальний сніданок:\n1/4 білок + 1/4 жири + 1/8 вуглеводи + 1/2 овочі/ягоди\n\nУ твоєму меню кожен сніданок вже збалансований саме так 🌿"},
    ]},
    9: {"theme": "☀️ Обід", "posts": [
        {"day": 1, "title": "Формула обіду",
         "text": "☀️ <b>Тиждень 9 · День 1</b>\n\n1/4 білок + 1/4 вуглеводи (найбільше за день) + мінімум жирів + 1/2 овочі\n\nОбід — найкращий час для складних вуглеводів 💚"},
    ]},
    10: {"theme": "🌙 Вечеря", "posts": [
        {"day": 1, "title": "Легка вечеря",
         "text": "🌙 <b>Тиждень 10 · День 1</b>\n\n1/3 нежирний білок + 1/6 складні вуглеводи + 1/2 овочі\n\nВечеря — мінімум жирів і вуглеводів 🌿"},
    ]},
    11: {"theme": "💧 Гідратація", "posts": [
        {"day": 1, "title": "Вода і обмін речовин",
         "text": "💧 <b>Тиждень 11 · День 1</b>\n\nНорма: 30-35 мл на кг ваги\n\nОзнаки нестачі: втома, хибний голод, запори\n\nЛайфхак: склянка води перед їжею зменшить порцію 💚"},
    ]},
    12: {"theme": "🏋️ Харчування і тренування", "posts": [
        {"day": 1, "title": "До та після тренування",
         "text": "🏋️ <b>Тиждень 12 · День 1</b>\n\nДо (за 1-2 год): складні вуглеводи + білок\nПісля (30-60 хв): білок + прості вуглеводи\n\nВуглеводи після тренування відновлюють глікоген 💪"},
    ]},
    13: {"theme": "🥗 Клітковина", "posts": [
        {"day": 1, "title": "25-35г клітковини на день",
         "text": "🌿 <b>Тиждень 13 · День 1</b>\n\nДжерела: овочі, фрукти, крупи, бобові\n\nКлітковина годує бактерії, знижує цукор, дає ситість\n\nТвоє меню вже містить потрібну кількість 💚"},
    ]},
    14: {"theme": "🍫 Солодке без шкоди", "posts": [
        {"day": 1, "title": "Десерт без провини",
         "text": "🍫 <b>Тиждень 14 · День 1</b>\n\nСолодке після сніданку або обіду — норма!\n\nЗаборона → зрив → провина\nДозвіл → задоволення → контроль\n\nВ меню є місце для маленьких радостей 🌿"},
    ]},
    15: {"theme": "⚖️ Управління вагою", "posts": [
        {"day": 1, "title": "Дефіцит без стресу",
         "text": "⚖️ <b>Тиждень 15 · День 1</b>\n\nБезпечний дефіцит: 300-500 ккал від норми\nТемп: 0.5-1 кг на тиждень\n\nТвоє меню вже враховує твою ціль 📊"},
    ]},
    16: {"theme": "⚖️ Гормони", "posts": [
        {"day": 1, "title": "Гормони і харчування",
         "text": "⚖️ <b>Тиждень 16 · День 1</b>\n\nІнсулін, лептин, кортизол — всі залежать від раціону.\n\nЗбалансована тарілка + регулярні прийоми = гормональний баланс 🌿"},
    ]},
    17: {"theme": "☀️ Вітамін D", "posts": [
        {"day": 1, "title": "Дефіцит D у більшості",
         "text": "☀️ <b>Тиждень 17 · День 1</b>\n\nДефіцит → втома, депресія, слабкий імунітет\n\nДжерела: сонце, жирна риба, яйця, D3\nОптимум: 50-80 нг/мл 💚"},
    ]},
    18: {"theme": "💊 Залізо", "posts": [
        {"day": 1, "title": "Залізодефіцит",
         "text": "🩸 <b>Тиждень 18 · День 1</b>\n\nОзнаки: втома, блідість, випадіння волосся\n\nДжерела: червоне м'ясо, шпинат, гречка\nЇж з вітаміном С — засвоєння +50% 🌿"},
    ]},
    19: {"theme": "😤 Стрес і кортизол", "posts": [
        {"day": 1, "title": "Кортизол і вага",
         "text": "😤 <b>Тиждень 19 · День 1</b>\n\nКортизол → тяга до солодкого, жир на животі\n\nЩо допомагає:\n✅ Сон 7-9 год\n✅ Регулярне харчування по меню\n✅ Прогулянки і рух 🌿"},
    ]},
    20: {"theme": "🍱 Meal Prep", "posts": [
        {"day": 1, "title": "Готовлю раз на тиждень",
         "text": "📦 <b>Тиждень 20 · День 1</b>\n\nMeal Prep за 2 години:\n1. Відвари 3 крупи\n2. Запечи білок\n3. Наріж овочі\n\nТвоє меню в боті вже є готовий план на тиждень 💚"},
    ]},
    21: {"theme": "🎯 Харчові цілі", "posts": [
        {"day": 1, "title": "SMART-цілі",
         "text": "🎯 <b>Тиждень 21 · День 1</b>\n\nНе: хочу харчуватися здоровіше\nТак: їстиму 5 порцій овочів щодня 4 тижні\n\nТвоя ціль вже встановлена в КБЖУ 🌿"},
    ]},
    22: {"theme": "🧪 Аналізи", "posts": [
        {"day": 1, "title": "Які аналізи здати",
         "text": "🧪 <b>Тиждень 22 · День 1</b>\n\n🩸 Загальний аналіз крові\n⚗️ Глюкоза, холестерин\n🦋 ТТГ (щитовидна)\n☀️ Вітамін D, B12\n\nЗдавай раз на 6 місяців 💚"},
    ]},
    23: {"theme": "🌍 Їжа поза домом", "posts": [
        {"day": 1, "title": "Кафе без стресу",
         "text": "🍽 <b>Тиждень 23 · День 1</b>\n\n✅ Шукай білок в меню\n✅ Проси соус окремо\n✅ Заміни картоплю фрі на овочі\n\nПринципи твого меню працюють скрізь 🌿"},
    ]},
    24: {"theme": "📱 Трекінг", "posts": [
        {"day": 1, "title": "Рахувати калорії?",
         "text": "📱 <b>Тиждень 24 · День 1</b>\n\nКоли корисно: хочеш зрозуміти раціон\nКоли шкідливо: викликає тривогу\n\nАльтернатива: метод тарілки + трекер в боті 💚"},
    ]},
    25: {"theme": "🏃 Енергія", "posts": [
        {"day": 1, "title": "Чому немає енергії?",
         "text": "⚡ <b>Тиждень 25 · День 1</b>\n\nПричини: переїдання, прості вуглеводи, мало білку\n\nЩо дає енергію:\n✅ Збалансована тарілка по меню\n✅ Регулярні прийоми\n✅ 1.5-2л води 🌿"},
    ]},
    26: {"theme": "🌟 Підтримка ваги", "posts": [
        {"day": 1, "title": "Як не зірватися",
         "text": "🌟 <b>Тиждень 26 · День 1</b>\n\nСистема звичок сильніша за силу волі.\n\n✅ Дотримуйся меню\n✅ Регулярно перевіряй прогрес\n✅ Повертайся до базового протоколу 💚"},
    ]},
    27: {"theme": "🎁 Свята", "posts": [
        {"day": 1, "title": "Свята без зривів",
         "text": "🎉 <b>Тиждень 27 · День 1</b>\n\n✅ Не голодуй до святкового столу\n✅ Починай з овочів\n✅ Їж повільно\n✅ Повернись до меню наступного ранку 🌿"},
    ]},
    28: {"theme": "🏆 Підсумок", "posts": [
        {"day": 1, "title": "28 тижнів — ти зробив це!",
         "text": "🏆 <b>Тиждень 28 · Фінал</b>\n\nТи пройшов весь курс NutriSense!\n\n✅ Розумієш своє тіло\n✅ Знаєш свої нутрієнти\n✅ Маєш систему харчування\n\n<b>Харчування — це не дієта. Це твій спосіб жити.</b>\n\nNutriSense завжди поруч 🌿💚"},
    ]},
}

# ─── РОУТЕР ────────────────────────────────────────────────────────
router = Router()

@router.message(CommandStart())
async def cmd_start(msg: Message):
    await upsert_user(
        msg.from_user.id,
        username=msg.from_user.username or "",
        full_name=msg.from_user.full_name or "",
    )
    user = await get_user(msg.from_user.id)
    plan = user.get("plan", "free") if user else "free"
    await msg.answer(
        "🌿 <b>Вітаю в NutriSense!</b>\n\n"
        "Я допоможу тобі харчуватися збалансовано — без дієт і заборон.\n\n"
        "Що я вмію:\n"
        "📊 Розраховую точну норму КБЖУ\n"
        "🍽 Складаю персональне меню на тиждень\n"
        "🧠 Визначаю тип харчової поведінки\n"
        "📚 Навчаю нутриціології 28 тижнів\n\n"
        "<b>Почни з розрахунку КБЖУ — це безкоштовно!</b>",
        reply_markup=kb_main_menu(plan)
    )

@router.message(Command("menu"))
async def cmd_menu(msg: Message):
    user = await get_user(msg.from_user.id)
    plan = user.get("plan", "free") if user else "free"
    await msg.answer("🏠 Головне меню:", reply_markup=kb_main_menu(plan))

@router.message(Command("admin"))
async def cmd_admin(msg: Message):
    if not is_admin(msg.from_user.id):
        return
    await msg.answer("🔐 <b>Адмін панель NutriSense</b>", reply_markup=kb_admin())

@router.message(Command("kbju"))
async def cmd_kbju(msg: Message, state: FSMContext):
    await state.set_state(KBJUState.waiting_gender)
    await msg.answer("📊 <b>Розрахунок КБЖУ</b>\n\nОбери стать:", reply_markup=kb_gender())

# ─── КБЖУ КАЛЬКУЛЯТОР ──────────────────────────────────────────────
@router.callback_query(F.data == "kbju_start")
async def cb_kbju_start(cq: CallbackQuery, state: FSMContext):
    await state.set_state(KBJUState.waiting_gender)
    await cq.message.edit_text("📊 <b>Розрахунок КБЖУ</b>\n\nОбери стать:", reply_markup=kb_gender())
    await cq.answer()

@router.callback_query(F.data.startswith("gender_"), KBJUState.waiting_gender)
async def kbju_gender(cq: CallbackQuery, state: FSMContext):
    gender = cq.data.split("_")[1]
    await state.update_data(gender=gender)
    await state.set_state(KBJUState.waiting_age)
    await cq.message.edit_text("Введи свій <b>вік</b> (наприклад: 27):")
    await cq.answer()

@router.message(KBJUState.waiting_age)
async def kbju_age(msg: Message, state: FSMContext):
    try:
        age = int(msg.text.strip())
        if not 10 <= age <= 100:
            raise ValueError
        await state.update_data(age=age)
        await state.set_state(KBJUState.waiting_weight)
        await msg.answer("Введи свою <b>вагу</b> (кг, наприклад: 65):")
    except ValueError:
        await msg.answer("⚠️ Введи коректний вік (10-100):")

@router.message(KBJUState.waiting_weight)
async def kbju_weight(msg: Message, state: FSMContext):
    try:
        weight = float(msg.text.strip().replace(",", "."))
        if not 30 <= weight <= 300:
            raise ValueError
        await state.update_data(weight=weight)
        await state.set_state(KBJUState.waiting_height)
        await msg.answer("Введи свій <b>зріст</b> (см, наприклад: 168):")
    except ValueError:
        await msg.answer("⚠️ Введи коректну вагу (30-300 кг):")

@router.message(KBJUState.waiting_height)
async def kbju_height(msg: Message, state: FSMContext):
    try:
        height = float(msg.text.strip().replace(",", "."))
        if not 100 <= height <= 250:
            raise ValueError
        await state.update_data(height=height)
        await state.set_state(KBJUState.waiting_activity)
        await msg.answer("Обери рівень <b>активності</b>:", reply_markup=kb_activity())
    except ValueError:
        await msg.answer("⚠️ Введи коректний зріст (100-250 см):")

@router.callback_query(F.data.startswith("activity_"), KBJUState.waiting_activity)
async def kbju_activity(cq: CallbackQuery, state: FSMContext):
    key = cq.data.replace("activity_", "")
    coef, label = ACTIVITY_LEVELS[key]
    await state.update_data(activity=coef, activity_label=label)
    await state.set_state(KBJUState.waiting_goal)
    await cq.message.edit_text("Яка твоя <b>ціль</b>?", reply_markup=kb_goal())
    await cq.answer()

@router.callback_query(F.data.startswith("goal_"), KBJUState.waiting_goal)
async def kbju_goal(cq: CallbackQuery, state: FSMContext):
    goal = cq.data.replace("goal_", "")
    data = await state.get_data()
    await state.clear()
    result = calculate_kbju(
        data["gender"], data["age"], data["weight"],
        data["height"], data["activity"], goal
    )
    await upsert_user(
        cq.from_user.id,
        gender=data["gender"], age=data["age"],
        weight=data["weight"], height=data["height"],
        activity=data["activity"], goal=goal,
        calories=result["calories"], protein=result["protein"],
        fat=result["fat"], carbs=result["carbs"],
    )
    user = await get_user(cq.from_user.id)
    plan = user.get("plan", "free") if user else "free"
    goal_label = GOALS[goal][0]
    gender_label = "Жінка" if data["gender"] == "female" else "Чоловік"

    text = (
        f"✅ <b>Твій результат КБЖУ</b>\n\n"
        f"👤 {gender_label}, {data['age']} р., {data['weight']} кг\n"
        f"🎯 Ціль: {goal_label}\n\n"
        f"🔥 Норма калорій: <b>{result['calories']} ккал/день</b>\n"
        f"🥩 Білок: <b>{result['protein']} г</b>\n"
        f"🧈 Жири: <b>{result['fat']} г</b>\n"
        f"🌾 Вуглеводи: <b>{result['carbs']} г</b>\n\n"
    )

    if plan == "free":
        text += (
            "━━━━━━━━━━━━━━━━━━━━\n"
            "💡 <b>Що далі?</b>\n\n"
            "Знати норму КБЖУ — це перший крок.\n"
            "Але без готового меню більшість повертається до старих звичок.\n\n"
            "З підпискою <b>Start</b> ти отримаєш:\n"
            "🍽 Готове меню на кожен день тижня\n"
            "📚 28 тижнів нутриціологічних знань\n"
            "🧠 Персональний аналіз харчової поведінки\n\n"
            "👇 Обери дію:"
        )
    else:
        text += (
            "━━━━━━━━━━━━━━━━━━━━\n"
            "🍽 Меню вже готове під твої показники!\n"
            "👇 Обери дію:"
        )

    await cq.message.edit_text(text, reply_markup=kb_after_kbju(plan))
    await cq.answer("✅ Розрахунок готовий!")

# ─── МЕНЮ ──────────────────────────────────────────────────────────
@router.callback_query(F.data == "menu_today")
async def cb_menu_today(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "start"):
        await cq.message.edit_text(
            "🔒 <b>Меню доступне з тарифу Start</b>\n\n"
            "Отримай готове меню на кожен день тижня,\n"
            "складене під твої показники КБЖУ.\n\n"
            "Вартість: всього <b>199 грн/міс</b>",
            reply_markup=kb_plans()
        )
        await cq.answer()
        return

    if not user.get("calories"):
        await cq.answer("⚠️ Спочатку розрахуй КБЖУ!", show_alert=True)
        return

    plan = user.get("plan", "free")
    day_num = user.get("menu_day", 1) or 1
    today_weekday = datetime.now().weekday() + 1
    if today_weekday > 7:
        today_weekday = 1

    week_num = user.get('week', 1) or 1
    menu_text = generate_menu_for_user(user, today_weekday, plan, week_num)
    await cq.message.edit_text(menu_text, reply_markup=kb_menu_actions(plan))
    await cq.answer()

@router.callback_query(F.data == "menu_week")
async def cb_menu_week(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "start"):
        await cq.answer("🔒 Доступно з Start", show_alert=True)
        return

    plan = user.get("plan", "free")
    week_num = user.get("week", 1) or 1
    menu_week_idx = ((week_num - 1) % 4) + 1
    current_menu = MENU_ROTATION.get(menu_week_idx, MENU_ROTATION[1])

    text = f"📅 <b>Меню на тиждень (меню #{menu_week_idx})</b>\n\n"
    for day_num in range(1, 8):
        day_data = current_menu.get(day_num, current_menu[1])
        text += f"<b>{day_data['day']}</b>\n"
        text += f"🌅 {day_data['breakfast'][0]}\n"
        text += f"☀️ {day_data['lunch'][0]}\n"
        text += f"🌙 {day_data['dinner'][0]}\n"
        if plan in ("premium", "vip"):
            text += f"🍎 {day_data['snack1'][0]}\n"
        text += "\n"

    b = InlineKeyboardBuilder()
    if plan in ("premium", "vip"):
        b.button(text="🔄 Обрати інший день", callback_data="menu_change")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)

    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

@router.callback_query(F.data == "menu_next")
async def cb_menu_next(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "start"):
        await cq.answer("🔒 Доступно з Start", show_alert=True)
        return

    current_day = datetime.now().weekday() + 1
    next_day = current_day + 1 if current_day < 7 else 1
    plan = user.get("plan", "free")
    week_num = user.get("week", 1) or 1
    menu_week_idx = ((week_num - 1) % 4) + 1
    current_menu = MENU_ROTATION.get(menu_week_idx, MENU_ROTATION[1])
    day_name = current_menu.get(next_day, current_menu[1])["day"]
    menu_text = generate_menu_for_user(user, next_day, plan, week_num)
    await cq.message.edit_text(
        f"➡️ <b>Меню на завтра ({day_name})</b>\n\n" + menu_text,
        reply_markup=kb_menu_actions(plan)
    )
    await cq.answer()

@router.callback_query(F.data == "menu_change")
async def cb_menu_change(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "premium"):
        await cq.message.edit_text(
            "🔒 <b>Зміна меню доступна з Premium</b>\n\n"
            "З Premium ти можеш:\n"
            "🔄 Змінити будь-яку страву\n"
            "🍱 Отримати альтернативні варіанти\n"
            "📅 Налаштувати меню під свої смаки\n\n"
            "Вартість: <b>349 грн/міс</b>",
            reply_markup=kb_plans()
        )
        await cq.answer()
        return

    b = InlineKeyboardBuilder()
    week_num = user.get("week", 1) or 1
    menu_week_idx = ((week_num - 1) % 4) + 1
    current_menu = MENU_ROTATION.get(menu_week_idx, MENU_ROTATION[1])
    for day_num in range(1, 8):
        day_data = current_menu.get(day_num, current_menu[1])
        b.button(text=f"📅 {day_data['day']}", callback_data=f"menu_show_{day_num}")
    b.button(text="◀️ Назад", callback_data="menu_today")
    b.adjust(2)

    await cq.message.edit_text(
        "🔄 <b>Оберіть день для перегляду меню:</b>\n\n"
        "Можеш переглянути меню будь-якого дня тижня",
        reply_markup=b.as_markup()
    )
    await cq.answer()

@router.callback_query(F.data.startswith("menu_show_"))
async def cb_menu_show_day(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    day_num = int(cq.data.replace("menu_show_", ""))
    plan = user.get("plan", "free")
    week_num = user.get("week", 1) or 1
    menu_text = generate_menu_for_user(user, day_num, plan, week_num)
    await cq.message.edit_text(menu_text, reply_markup=kb_menu_actions(plan))
    await cq.answer()

# ─── ПРОГРЕС ────────────────────────────────────────────────────────
@router.callback_query(F.data == "my_progress")
async def cb_my_progress(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "premium"):
        await cq.answer("🔒 Доступно з Premium", show_alert=True)
        return

    week = user.get("week", 1) or 1
    joined = user.get("joined_at", "")[:10] if user.get("joined_at") else "—"
    plan = PLANS.get(user.get("plan", "free"), {}).get("name", "Free")
    until = user.get("plan_until", "")[:10] if user.get("plan_until") else "—"

    days_on_plan = 0
    if user.get("joined_at"):
        try:
            joined_dt = datetime.strptime(user["joined_at"][:10], "%Y-%m-%d")
            days_on_plan = (datetime.now() - joined_dt).days
        except Exception:
            pass

    text = (
        f"📈 <b>Мій прогрес</b>\n\n"
        f"📅 В NutriSense: {days_on_plan} днів\n"
        f"📚 Тиждень навчання: {week}/28\n"
        f"💎 Тариф: {plan} (до {until})\n\n"
        f"🎯 Ціль: {GOALS.get(user.get('goal','maintain'), ('—',))[0]}\n"
        f"🔥 Норма: {user.get('calories', '—')} ккал/день\n\n"
    )

    progress_bar = "🟩" * min(week, 10) + "⬜" * (10 - min(week, 10))
    text += f"Прогрес курсу: {progress_bar}\n{week}/28 тижнів\n\n"

    if week < 28:
        text += f"💡 Залишилось {28 - week} тижнів до завершення курсу!"
    else:
        text += "🏆 Курс завершено! Ти молодець!"

    b = InlineKeyboardBuilder()
    b.button(text="🍽 Меню на сьогодні", callback_data="menu_today")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)

    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

# ─── ТЕСТ ПОВЕДІНКИ ─────────────────────────────────────────────────
@router.callback_query(F.data == "behavior_test")
async def cb_behavior_test(cq: CallbackQuery, state: FSMContext):
    await state.set_state(BehaviorTestState.waiting_answer)
    await state.update_data(answers=[], q_num=0)
    await cq.message.edit_text(
        "🧠 <b>Тест харчової поведінки</b>\n\n"
        "5 питань — і ти дізнаєшся свій тип харчової поведінки "
        "та отримаєш персональні рекомендації.\n\n"
        f"{BEHAVIOR_QUESTIONS[0]}",
        reply_markup=kb_behavior_test(0)
    )
    await cq.answer()

@router.callback_query(BehaviorTestState.waiting_answer, F.data.startswith("bt_"))
async def cb_behavior_answer(cq: CallbackQuery, state: FSMContext):
    data = await state.get_data()
    answers = data.get("answers", [])
    q_num = data.get("q_num", 0)
    answers.append(cq.data)
    q_num += 1

    if q_num >= len(BEHAVIOR_QUESTIONS):
        await state.clear()
        emoji, type_name, description = analyze_behavior(answers)
        user = await get_user(cq.from_user.id)
        plan = user.get("plan", "free") if user else "free"

        text = (
            f"{emoji} <b>Результат: {type_name}</b>\n\n"
            f"{description}\n\n"
            "━━━━━━━━━━━━━━━━━━━━\n"
        )

        if plan == "free":
            text += (
                "💡 <b>Наступний крок:</b>\n\n"
                "Знаючи свій тип — важливо мати структуру харчування.\n"
                "Готове меню на тиждень допоможе уникнути хаосу.\n\n"
                "Спробуй <b>Start</b> — 199 грн/міс"
            )
        else:
            text += "🍽 Твоє меню вже враховує ці особливості!"

        b = InlineKeyboardBuilder()
        b.button(text="🍽 Меню на сьогодні", callback_data="menu_today")
        if plan == "free":
            b.button(text="🔓 Отримати меню", callback_data="plans")
        b.button(text="🏠 Головне меню", callback_data="main_menu")
        b.adjust(1)

        await cq.message.edit_text(text, reply_markup=b.as_markup())
    else:
        await state.update_data(answers=answers, q_num=q_num)
        await cq.message.edit_text(
            f"<b>Питання {q_num+1}/5</b>\n\n{BEHAVIOR_QUESTIONS[q_num]}",
            reply_markup=kb_behavior_test(q_num)
        )
    await cq.answer()

# ─── ТАРИФИ І ОПЛАТА ───────────────────────────────────────────────
@router.callback_query(F.data == "main_menu")
async def cb_main_menu(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    plan = user.get("plan", "free") if user else "free"
    await cq.message.edit_text("🏠 <b>Головне меню NutriSense</b>", reply_markup=kb_main_menu(plan))
    await cq.answer()

@router.callback_query(F.data == "plans")
async def cb_plans(cq: CallbackQuery):
    text = "💎 <b>Тарифи NutriSense</b>\n\n"
    for key, plan in PLANS.items():
        text += f"{plan['emoji']} <b>{plan['name']}</b> — {plan['price_uah']} грн/міс\n"
        for f in plan["features"]:
            text += f"  {f}\n"
        text += "\n"
    text += "👇 Обери тариф:"
    await cq.message.edit_text(text, reply_markup=kb_plans())
    await cq.answer()

@router.callback_query(F.data.startswith("buy_"))
async def cb_buy(cq: CallbackQuery):
    plan = cq.data.replace("buy_", "")
    if plan not in PLANS:
        await cq.answer("⚠️ Помилка", show_alert=True)
        return
    p = PLANS[plan]
    text = (
        f"{p['emoji']} <b>{p['name']} — {p['price_uah']} грн/міс</b>\n\n"
        f"📅 Доступ: 30 днів\n\n"
        f"<b>Включає:</b>\n"
        + "\n".join(f"  {f}" for f in p["features"])
        + "\n\nОплата через Monobank 💳"
    )
    await cq.message.edit_text(text, reply_markup=kb_confirm_buy(plan))
    await cq.answer()

@router.callback_query(F.data.startswith("pay_card_"))
async def cb_pay_card(cq: CallbackQuery):
    plan = cq.data.replace("pay_card_", "")
    if plan not in PLANS:
        await cq.answer("⚠️ Помилка", show_alert=True)
        return
    p = PLANS[plan]
    user = cq.from_user
    await save_payment(
        user_id=user.id, plan=plan, amount=p["price_uah"],
        invoice_id=f"jar_{user.id}_{plan}_{int(datetime.now().timestamp())}"
    )
    try:
        b_admin = InlineKeyboardBuilder()
        b_admin.button(
            text=f"✅ Активувати {p['name']}",
            callback_data=f"adm_oneclick_{user.id}_{plan}"
        )
        b_admin.button(text="❌ Відхилити", callback_data=f"adm_reject_{user.id}")
        b_admin.adjust(1)
        await cq.bot.send_message(
            ADMIN_ID,
            f"💰 <b>Новий запит на оплату!</b>\n\n"
            f"👤 {user.full_name} (@{user.username or '-'})\n"
            f"💎 Тариф: {p['name']}\n"
            f"💵 Сума: {p['price_uah']} грн",
            reply_markup=b_admin.as_markup()
        )
    except Exception as e:
        log.warning(f"Cannot notify admin: {e}")

    b = InlineKeyboardBuilder()
    b.button(text="💳 Перейти до оплати", url=MONO_JAR_URL)
    b.button(text="✅ Я оплатив(ла)", callback_data=f"paid_notify_{plan}")
    b.button(text="◀️ Назад", callback_data="plans")
    b.adjust(1)
    await cq.message.edit_text(
        f"💳 <b>Оплата {p['name']}</b>\n\n"
        f"💵 Сума: <b>{p['price_uah']} грн</b>\n\n"
        f"1️⃣ Натисни <b>Перейти до оплати</b>\n"
        f"2️⃣ Переведи рівно <b>{p['price_uah']} грн</b>\n"
        f"3️⃣ Натисни <b>Я оплатив(ла)</b>\n"
        f"4️⃣ Підписка активується за кілька хвилин ⚡",
        reply_markup=b.as_markup()
    )
    await cq.answer()

@router.callback_query(F.data.startswith("paid_notify_"))
async def cb_paid_notify(cq: CallbackQuery):
    plan = cq.data.replace("paid_notify_", "")
    user = cq.from_user
    p = PLANS.get(plan, {})
    b = InlineKeyboardBuilder()
    b.button(
        text=f"✅ Активувати {p.get('name', plan)}",
        callback_data=f"adm_oneclick_{user.id}_{plan}"
    )
    b.button(text="❌ Відхилити", callback_data=f"adm_reject_{user.id}")
    b.adjust(1)
    try:
        await cq.bot.send_message(
            ADMIN_ID,
            f"🔔 <b>Клієнт каже що оплатив!</b>\n\n"
            f"👤 {user.full_name} (@{user.username or '-'})\n"
            f"💎 Тариф: {p.get('name', plan)}\n"
            f"💵 Сума: {p.get('price_uah', '?')} грн\n\n"
            f"Перевір банку і натисни 👇",
            reply_markup=b.as_markup()
        )
    except Exception as e:
        log.warning(f"Cannot notify admin: {e}")
    await cq.message.edit_text(
        "✅ <b>Дякуємо!</b>\n\n"
        "Отримали сповіщення і перевіряємо оплату.\n"
        "Підписка активується за кілька хвилин.\n\n"
        "Поки чекаєш — можеш пройти тест харчової поведінки 🌿",
        reply_markup=InlineKeyboardBuilder().button(
            text="🧠 Пройти тест", callback_data="behavior_test"
        ).as_markup()
    )
    await cq.answer("✅ Сповіщення надіслано!")

@router.callback_query(F.data.startswith("adm_oneclick_"))
async def adm_oneclick_activate(cq: CallbackQuery):
    if not is_admin(cq.from_user.id):
        return
    parts = cq.data.split("_")
    user_id = int(parts[2])
    plan = parts[3]
    plan_until = (datetime.now() + timedelta(days=PLANS[plan]["days"])).strftime("%Y-%m-%d %H:%M:%S")
    await upsert_user(user_id, plan=plan, plan_until=plan_until)
    try:
        await cq.bot.send_message(
            user_id,
            f"🎉 <b>Підписку активовано!</b>\n\n"
            f"Тариф: {PLANS[plan]['name']}\n"
            f"Діє до: {plan_until[:10]}\n\n"
            f"🍽 Твоє меню вже готове!\n"
            f"/menu — перейти до меню"
        )
    except Exception as e:
        log.warning(f"Cannot notify user {user_id}: {e}")
    await cq.message.edit_text(
        f"✅ Активовано!\nЮзер {user_id} — {PLANS[plan]['name']}\nДо: {plan_until[:10]}"
    )
    await cq.answer("✅ Підписку активовано!")

@router.callback_query(F.data.startswith("adm_reject_"))
async def adm_reject_payment(cq: CallbackQuery):
    if not is_admin(cq.from_user.id):
        return
    user_id = int(cq.data.split("_")[2])
    try:
        await cq.bot.send_message(
            user_id,
            "⚠️ <b>Оплату не знайдено.</b>\n\nПеревір чи правильно переведена сума або напиши адміністратору."
        )
    except Exception:
        pass
    await cq.message.edit_text(f"❌ Оплату юзера {user_id} відхилено.")
    await cq.answer("❌ Відхилено")

# ─── ТРЕКЕР ────────────────────────────────────────────────────────
@router.callback_query(F.data == "tracker")
async def cb_tracker(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "premium"):
        await cq.message.edit_text(
            "🔒 <b>Трекер доступний з Premium</b>\n\n"
            "Фіксуй прийоми їжі і отримуй щотижневий звіт прогресу.\n\n"
            "Вартість: <b>349 грн/міс</b>",
            reply_markup=kb_plans()
        )
        await cq.answer()
        return

    today = datetime.now().strftime("%Y-%m-%d")
    async with aiosqlite.connect(DB_PATH) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute(
            "SELECT * FROM tracker WHERE user_id = ? AND date = ?",
            (cq.from_user.id, today)
        ) as cur:
            entries = await cur.fetchall()

    text = f"📅 <b>Трекер їжі — {today}</b>\n\n"
    if entries:
        total_meals = len(entries)
        text += f"Записано прийомів: {total_meals}\n\n"
        for e in entries:
            text += f"• {e['meal']}: {e['note']}\n"
        text += "\n💡 Надсилай прийоми у форматі:\n<code>Сніданок: вівсянка, яйця, ягоди</code>"
    else:
        text += (
            "Сьогодні записів немає.\n\n"
            "Надсилай прийоми їжі у форматі:\n"
            "<code>Сніданок: вівсянка, яйця, ягоди</code>\n\n"
            "💡 Трекер допомагає бачити реальну картину харчування"
        )

    b = InlineKeyboardBuilder()
    b.button(text="🍽 Моє меню", callback_data="menu_today")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)
    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

@router.message(F.text.regexp(r"^(Сніданок|Обід|Вечеря|Перекус):.+"))
async def handle_tracker_entry(msg: Message):
    user = await get_user(msg.from_user.id)
    if not has_access(user, "premium"):
        return
    parts = msg.text.split(":", 1)
    meal = parts[0].strip()
    note = parts[1].strip() if len(parts) > 1 else ""
    today = datetime.now().strftime("%Y-%m-%d")
    async with aiosqlite.connect(DB_PATH) as db:
        await db.execute(
            "INSERT INTO tracker (user_id, date, meal, note) VALUES (?,?,?,?)",
            (msg.from_user.id, today, meal, note)
        )
        await db.commit()
    b = InlineKeyboardBuilder()
    b.button(text="📅 Переглянути трекер", callback_data="tracker")
    b.button(text="🍽 Моє меню", callback_data="menu_today")
    b.adjust(1)
    await msg.answer(
        f"✅ <b>Записано!</b>\n\n"
        f"🍽 {meal}: {note}\n\n"
        f"Продовжуй фіксувати прийоми їжі 💪",
        reply_markup=b.as_markup()
    )

# ─── ПРОФІЛЬ ───────────────────────────────────────────────────────
@router.callback_query(F.data == "my_profile")
async def cb_my_profile(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not user:
        await cq.answer("⚠️ Профіль не знайдено", show_alert=True)
        return
    plan_name = PLANS.get(user["plan"], {}).get("name", "Free") if user["plan"] != "free" else "Free"
    until_str = user.get("plan_until", "")
    if until_str:
        until = datetime.strptime(until_str, "%Y-%m-%d %H:%M:%S")
        days_left = max(0, (until - datetime.now()).days)
        until_label = f"{until.strftime('%d.%m.%Y')} (ще {days_left} дн.)"
    else:
        until_label = "—"

    text = (
        f"👤 <b>Мій профіль</b>\n\n"
        f"💎 Тариф: {plan_name}\n"
        f"📅 Діє до: {until_label}\n"
        f"📚 Тиждень курсу: {user.get('week', 1)}/28\n\n"
    )
    if user.get("calories"):
        goal_label = GOALS.get(user["goal"], ("—",))[0]
        text += (
            f"<b>📊 Мій КБЖУ:</b>\n"
            f"🔥 {user['calories']} ккал/день\n"
            f"🥩 Білок: {user['protein']}г  "
            f"🧈 Жири: {user['fat']}г  "
            f"🌾 Вугл.: {user['carbs']}г\n"
            f"🎯 Ціль: {goal_label}\n"
        )

    b = InlineKeyboardBuilder()
    b.button(text="🔄 Перерахувати КБЖУ", callback_data="kbju_start")
    b.button(text="🍽 Моє меню", callback_data="menu_today")
    b.button(text="💎 Продовжити підписку", callback_data="plans")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(2)
    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

# ─── КОНТЕНТ ───────────────────────────────────────────────────────
@router.callback_query(F.data == "weekly_content")
async def cb_weekly_content(cq: CallbackQuery):
    user = await get_user(cq.from_user.id)
    if not has_access(user, "start"):
        await cq.message.edit_text(
            "🔒 <b>Освітній контент доступний з Start</b>\n\n"
            "28 тижнів нутриціологічних знань:\n"
            "🧠 Харчова поведінка\n"
            "🥗 Макро і мікронутрієнти\n"
            "😴 Сон, стрес і вага\n"
            "🍽 Правила тарілки\n\n"
            "Вартість: <b>199 грн/міс</b>",
            reply_markup=kb_plans()
        )
        await cq.answer()
        return

    week_num = user.get("week", 1) or 1
    week_data = WEEKLY_CONTENT.get(week_num, WEEKLY_CONTENT[1])
    posts = week_data["posts"]

    text = (
        f"📚 <b>Тиждень {week_num}: {week_data['theme']}</b>\n\n"
        f"Матеріалів цього тижня: {len(posts)}\n"
        f"Прогрес: тиждень {week_num} з 28\n\n"
    )
    for p in posts:
        text += f"📌 {p['title']}\n"

    b = InlineKeyboardBuilder()
    for i, p in enumerate(posts):
        b.button(text=f"📖 {p['title'][:35]}", callback_data=f"read_post_{week_num}_{i}")
    b.button(text="🍽 Моє меню", callback_data="menu_today")
    b.button(text="🏠 Головне меню", callback_data="main_menu")
    b.adjust(1)
    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

@router.callback_query(F.data.startswith("read_post_"))
async def cb_read_post(cq: CallbackQuery):
    parts = cq.data.split("_")
    week_num, post_idx = int(parts[2]), int(parts[3])
    week_data = WEEKLY_CONTENT.get(week_num)
    if not week_data or post_idx >= len(week_data["posts"]):
        await cq.answer("⚠️ Не знайдено", show_alert=True)
        return
    post = week_data["posts"][post_idx]
    b = InlineKeyboardBuilder()
    if post_idx > 0:
        b.button(text="◀️ Попередній", callback_data=f"read_post_{week_num}_{post_idx-1}")
    if post_idx < len(week_data["posts"]) - 1:
        b.button(text="Наступний ▶️", callback_data=f"read_post_{week_num}_{post_idx+1}")
    b.button(text="🍽 Застосувати в меню", callback_data="menu_today")
    b.button(text="📚 До тижня", callback_data="weekly_content")
    b.adjust(2, 1)
    await cq.message.edit_text(post["text"], reply_markup=b.as_markup())
    await cq.answer()

# ─── АДМІН ─────────────────────────────────────────────────────────
@router.callback_query(F.data == "adm_stats")
async def adm_stats(cq: CallbackQuery):
    if not is_admin(cq.from_user.id):
        return
    stats = await get_stats()
    text = (
        f"📊 <b>Статистика NutriSense</b>\n\n"
        f"👥 Всього юзерів: {stats['total']}\n"
        f"💎 Платних: {stats['paid']}\n"
        f"🆓 Безкоштовних: {stats['total'] - stats['paid']}\n"
        f"💰 Дохід: {stats['revenue']} грн\n"
        f"📅 {datetime.now().strftime('%d.%m.%Y %H:%M')}"
    )
    b = InlineKeyboardBuilder()
    b.button(text="◀️ Назад", callback_data="adm_back")
    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

@router.callback_query(F.data == "adm_users")
async def adm_users(cq: CallbackQuery):
    if not is_admin(cq.from_user.id):
        return
    users = await get_all_users()
    text = f"👥 <b>Користувачі ({len(users)}):</b>\n\n"
    for u in users[:20]:
        plan = u.get("plan", "free")
        name = u.get("full_name") or u.get("username") or str(u["user_id"])
        text += f"• {name} | {plan} | тиж.{u.get('week', 1)}\n"
    if len(users) > 20:
        text += f"\n...і ще {len(users)-20}"
    b = InlineKeyboardBuilder()
    b.button(text="◀️ Назад", callback_data="adm_back")
    await cq.message.edit_text(text, reply_markup=b.as_markup())
    await cq.answer()

@router.callback_query(F.data == "adm_broadcast")
async def adm_broadcast(cq: CallbackQuery, state: FSMContext):
    if not is_admin(cq.from_user.id):
        return
    await state.set_state(AdminBroadcastState.waiting_text)
    await cq.message.edit_text("📢 <b>Розсилка</b>\n\nНадішли текст для всіх юзерів:")
    await cq.answer()

@router.message(AdminBroadcastState.waiting_text)
async def adm_do_broadcast(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    await state.clear()
    users = await get_all_users()
    sent = failed = 0
    for u in users:
        try:
            await msg.bot.send_message(u["user_id"], msg.text)
            sent += 1
        except Exception:
            failed += 1
    await msg.answer(
        f"✅ Розсилка завершена\n📤 Відправлено: {sent}\n❌ Помилок: {failed}",
        reply_markup=kb_admin()
    )

@router.callback_query(F.data == "adm_send_content")
async def adm_send_content(cq: CallbackQuery):
    if not is_admin(cq.from_user.id):
        return
    await cq.answer("⏳ Відправляю...")
    users = await get_all_users()
    sent = 0
    for u in users:
        if not has_access(u, "start"):
            continue
        week_num = u.get("week", 1) or 1
        week_data = WEEKLY_CONTENT.get(week_num)
        if not week_data or not week_data["posts"]:
            continue
        post = week_data["posts"][0]
        try:
            b = InlineKeyboardBuilder()
            b.button(text="📚 Всі матеріали тижня", callback_data="weekly_content")
            b.button(text="🍽 Моє меню", callback_data="menu_today")
            b.adjust(1)
            await cq.bot.send_message(
                u["user_id"],
                f"📚 <b>Новий тиждень в NutriSense!</b>\n\n"
                f"<b>Тиждень {week_num}: {week_data['theme']}</b>\n\n"
                + post["text"],
                reply_markup=b.as_markup()
            )
            await upsert_user(u["user_id"], week=min(week_num + 1, 28))
            sent += 1
        except Exception:
            pass
    await cq.message.edit_text(
        f"✅ Контент відправлено {sent} юзерам", reply_markup=kb_admin()
    )

@router.callback_query(F.data == "adm_back")
async def adm_back(cq: CallbackQuery):
    if not is_admin(cq.from_user.id):
        return
    await cq.message.edit_text("🔐 <b>Адмін панель NutriSense</b>", reply_markup=kb_admin())
    await cq.answer()

# ─── ПЛАНУВАЛЬНИК ──────────────────────────────────────────────────
async def schedule_weekly_content(bot: Bot):
    while True:
        now = datetime.now()
        days_until_monday = (7 - now.weekday()) % 7 or 7
        next_run = now.replace(hour=10, minute=0, second=0, microsecond=0)
        next_run += timedelta(days=days_until_monday)
        wait_seconds = (next_run - now).total_seconds()
        log.info(f"Next content: {next_run.strftime('%d.%m.%Y %H:%M')}")
        await asyncio.sleep(wait_seconds)
        users = await get_all_users()
        sent = 0
        for u in users:
            if not has_access(u, "start"):
                continue
            week_num = u.get("week", 1) or 1
            week_data = WEEKLY_CONTENT.get(week_num)
            if not week_data or not week_data["posts"]:
                continue
            post = week_data["posts"][0]
            try:
                b = InlineKeyboardBuilder()
                b.button(text="📚 Читати", callback_data="weekly_content")
                b.button(text="🍽 Моє меню", callback_data="menu_today")
                b.adjust(1)
                await bot.send_message(
                    u["user_id"],
                    f"📚 <b>Новий тиждень!</b>\n\n"
                    f"<b>Тиждень {week_num}: {week_data['theme']}</b>\n\n"
                    + post["text"],
                    reply_markup=b.as_markup()
                )
                await upsert_user(u["user_id"], week=min(week_num + 1, 28))
                sent += 1
            except Exception as e:
                log.warning(f"Cannot send to {u['user_id']}: {e}")
        log.info(f"Content sent: {sent} users")

# ─── MAIN ───────────────────────────────────────────────────────────
async def main():
    await init_db()
    bot = Bot(
        token=BOT_TOKEN,
        default=DefaultBotProperties(parse_mode=ParseMode.HTML)
    )
    dp = Dispatcher(storage=MemoryStorage())
    dp.include_router(router)
    asyncio.create_task(schedule_weekly_content(bot))
    log.info("NutriSense Bot v2 started")
    await bot.delete_webhook(drop_pending_updates=True)
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())